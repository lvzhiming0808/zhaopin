// import * as types from './actionTypes'
// import { publishWay, getDerpartment } from '../../../api/jobApi/newCreat' //  新招聘的发布的api
export default {
    // 获取发布职位列表
  // async [types.HANDEL_PUBLISHLIST]({ commit, state }) {
    // commit(types.HANDEL_PUBLISHLIST, value)
    // 等我执行完了再执行下边的东西
  //   const res = await publishWay()
  //   commit(types.HANDEL_PUBLISHLIST, res.result)
  // },
  // 所属部门
  // async [types.HANDEL_DEPARTMENT]({ commit }, value) {
  //   const res = await getDerpartment({
  //     channelId: 10003
  //   })
  //   commit(types.HANDEL_DEPARTMENT, res.result)
  // },
  // [types.HANDEL_RESUME]({ commit }, value) {
  //   commit(types.HANDEL_RESUME, value)
  // }
}
